#include "main.h"

/**
  * main - test function for positive or negative
  * Return: 0 (Success)
  */

int main(void)
{
	int i;

	 i = 0;
	if (c == 'q' || c == 'e')
	 positive_or_negative(i);

	return (0);
}
