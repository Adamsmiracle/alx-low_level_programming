#include "main.h"

/**
  * _abs - returns the absolute value of a number
  * @c: input integer
  * Return: the number
  */

int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
	{
		return (-c);
	}

}
